[gd_scene format=3 uid="uid://b6uungnqwl3d7"]

[ext_resource type="Script" uid="uid://drso0xrey28fu" path="res://Scripts/objeto_reparavel.gd" id="1_ep42t"]
[ext_resource type="PackedScene" uid="uid://dhaseijc23l3v" path="res://ModeloItens/ParedeNormal.glb" id="2_ep42t"]
[ext_resource type="Script" uid="uid://bficxmsgd58cs" path="res://ObjetosSubmarino/Reparaveis/fonte.gd" id="5_w653u"]
[ext_resource type="PackedScene" uid="uid://dxdyrxy6dqqni" path="res://ModeloItens/ParedeAberta4glb.glb" id="6_w653u"]

[sub_resource type="BoxShape3D" id="BoxShape3D_2g7dt"]
size = Vector3(0.69262695, 7.471466, 14.237854)

[sub_resource type="BoxShape3D" id="BoxShape3D_efr2g"]
size = Vector3(5.118286, 9.041138, 13.679581)

[sub_resource type="StandardMaterial3D" id="StandardMaterial3D_jrcvg"]
transparency = 1
albedo_color = Color(0, 0.94509804, 0.8, 0.22352941)

[sub_resource type="Shader" id="Shader_2g7dt"]
code = "shader_type spatial;
render_mode cull_disabled, depth_draw_always;

uniform vec4 albedo : source_color = vec4(0.45, 0.6, 0.85, 1.0);

// controls
uniform vec3 gravity = vec3(0.0, 1.0, 0.0); // local-space down
uniform float flow_speed = 1.5;   // how fast the slide moves (time multiplier)
uniform float flow_scale = 17.0;   // spatial frequency along the flow direction
uniform float amplitude = 35.0;   // max tangential slide magnitude
uniform float thickness = 0.00;   // extra offset along normal (optional sheet thickness)
uniform float duty_cycle = 15.00;  // fraction of the repeating cycle during which vertices move (0..1)
uniform float global_time_offset = 0.0; // per-instance offset to desync

// Bias direction parameters
uniform sampler2D bias_direction_texture : filter_linear;
uniform float bias_min_y = 0.0;
uniform float bias_max_y = 10.0;
uniform float bias_direction_strength : hint_range(0.0, 1.0, 0.1) = 1.0; // 0 = use gravity only, 1 = use bias only

uniform bool force_messy = false;

vec3 get_bias_direction(float world_y) {
    // Normalize Y coordinate to 0..1 range
    float t = clamp((world_y - bias_min_y) / (bias_max_y - bias_min_y), 0.0, 1.0);
    
    // Sample from texture (texture coordinate is t, 0.5)
    vec3 color_bias_dir = texture(bias_direction_texture, vec2(t, 0.5)).rgb;
	//convert from color back to dir
	vec3 bias_dir = color_bias_dir * 2.0 - 1.0;
	
	
    // Ensure it's normalized
    float len = length(bias_dir);
	
    if (len > 0.001) {
        return bias_dir / len;
    } else {
        return vec3(0.0, 1.0, 0.0);
    }
}

void vertex() {
    // Get world Y position for this vertex
    vec4 world_pos = MODEL_MATRIX * vec4(VERTEX, 1.0);
    float world_y = world_pos.y;
    
    // normalized local gravity & normal
    vec3 g = normalize(gravity);
    vec3 Nn = normalize(NORMAL);

    // Project gravity onto the tangent plane to get the flow direction
    vec3 proj = g - dot(g, Nn) * Nn;
    float proj_len = length(proj);

    // Fallback tangent if projection is near-zero (flat horizontal case)
    vec3 flow_dir;
    if (proj_len > 1e-5) {
        flow_dir = proj / proj_len;
    } else {
        // pick an arbitrary perpendicular direction
        flow_dir = normalize(cross(Nn, vec3(0.0, 1.0, 0.0)));
        if (length(flow_dir) < 1e-5) {
            flow_dir = normalize(cross(Nn, vec3(1.0, 0.0, 0.0)));
        }
    }

    
    // Get bias direction and mix with gravity-based flow
    vec3 bias_dir = get_bias_direction(world_y);
    
    // Project bias direction onto tangent plane as well
    vec3 bias_proj = bias_dir - dot(bias_dir, Nn) * Nn;
    float bias_proj_len = length(bias_proj);
    
    if (bias_proj_len > 1e-5) {
        bias_proj = bias_proj / bias_proj_len;
		
		if (force_messy){
			bias_dir = bias_proj;
		}
		
		
		
        // Mix the two directions
        flow_dir = normalize(mix(flow_dir, bias_dir, bias_direction_strength));
    }
    
    // Spatial coordinate along the flow direction (per-vertex)
    float flow_coord = dot(VERTEX, flow_dir) * flow_scale;

    // time and phase (sawtooth)
    float t = TIME + global_time_offset;
    float phase = fract(flow_coord -t * flow_speed); // repeating 0..1
	
    phase = clamp(phase, 0.0, 1.0);

    // Only move while phase is in the duty window; otherwise snap back to zero.
    float factor = 0.0;
    if (phase < duty_cycle && duty_cycle > 0.0001) {
        factor = phase / duty_cycle; // ramp 0 -> 1 across the duty window
    } else {
        factor = 0.0;
    }

    // Tangential displacement (along flow_dir) and small normal push for thickness
    vec3 tangential_offset = flow_dir * (factor * amplitude);
    vec3 normal_offset = Nn * (-factor * thickness);

    // Apply displacement
    VERTEX += tangential_offset + normal_offset;
}

void fragment() {
    // same normal vector for every face
    NORMAL = -normalize(cross(dFdx(VERTEX),dFdy(VERTEX)));
    
    ALBEDO = albedo.rgb;
    ALPHA = albedo.a;

    // simple fresnel-ish edge highlight for gloss
    float facing = clamp(1.0 - dot(NORMAL, VIEW), 0.0, 1.0);
    ALBEDO += vec3(0.08 * pow(facing, 2.0));
    METALLIC = 0.0;
    ROUGHNESS = 0.35;
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_s8fht"]
render_priority = 0
shader = SubResource("Shader_2g7dt")
shader_parameter/albedo = Color(0.25558573, 0.58018804, 0.8345147, 1)
shader_parameter/gravity = Vector3(0, 1, 0)
shader_parameter/flow_speed = 2.5
shader_parameter/flow_scale = 17.0
shader_parameter/amplitude = 6.5
shader_parameter/thickness = 0.0
shader_parameter/duty_cycle = 15.0
shader_parameter/global_time_offset = 0.0
shader_parameter/bias_min_y = 0.0
shader_parameter/bias_max_y = 10.0
shader_parameter/bias_direction_strength = 1.0
shader_parameter/force_messy = true

[node name="ParedeCasco" type="StaticBody3D" unique_id=1488084307 node_paths=PackedStringArray("modelo_normal", "modelo_quebrado")]
script = ExtResource("1_ep42t")
tempo_reparo = 2.0
modelo_normal = NodePath("ParedeNormal")
modelo_quebrado = NodePath("ParedeAberta4glb")

[node name="ParedeNormal" parent="." unique_id=229180872 instance=ExtResource("2_ep42t")]
transform = Transform3D(0.415, 0, 0, 0, 0.415, 0, 0, 0, 0.415, 0, 0, 0)

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=857441095]
transform = Transform3D(0.41499993, 0, 0, 0, 0.41499993, 0, 0, 0, 0.41499993, 0.010413568, 1.7374722, -0.058258057)
shape = SubResource("BoxShape3D_2g7dt")

[node name="AreaInteracao" type="Area3D" parent="." unique_id=426566297]
transform = Transform3D(0.415, 0, 0, 0, 0.415, 0, 0, 0, 0.415, 0, 4.4242077, -0.19782257)

[node name="CollisionShape3D" type="CollisionShape3D" parent="AreaInteracao" unique_id=125230756]
transform = Transform3D(0.41500005, 0, 0, 0, 0.41500005, 0, 0, 0, 0.41500005, -0.8169877, -5.9180346, 0.4782458)
shape = SubResource("BoxShape3D_efr2g")

[node name="CSGBox3D" type="CSGBox3D" parent="." unique_id=1388166727]
transform = Transform3D(0.4149999, 0, 0, 0, 0.4149999, 0, 0, 0, 0.4149999, -0.40878057, 2.2854986, -0.16267656)
visible = false
material_override = SubResource("StandardMaterial3D_jrcvg")
size = Vector3(0.26757813, 2.3711548, 2.526001)

[node name="ParedeAberta4glb" parent="." unique_id=2123950993 instance=ExtResource("6_w653u")]
transform = Transform3D(0.415, 0, 0, 0, 0.415, 0, 0, 0, 0.415, 0, 0, 0)

[node name="Fonte" parent="ParedeAberta4glb" index="1" unique_id=932384183]
surface_material_override/0 = SubResource("ShaderMaterial_s8fht")
script = ExtResource("5_w653u")

[editable path="ParedeAberta4glb"]
